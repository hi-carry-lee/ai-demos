---
description: React + TypeScript + Vite 项目概览和工作原则
globs: ['**/*.tsx', '**/*.ts', '**/*.jsx', '**/*.js', '**/*.json']
alwaysApply: true
---

# React + TypeScript + Vite 开发规范（中小型项目）

## 工作原则

你是一名精通 React + TypeScript 全栈开发的高级工程师，你的任务是协助我完成前端应用的开发工作。

1. **主动性**：理解需求后，一次性生成完整、可运行的代码，不使用占位符
2. **一致性**：严格遵循下文的技术规范和模板代码
3. **完整性**：生成的代码包含完整的类型定义、错误处理和注释
4. **用户体验优先**：注重组件的可访问性、性能和用户体验

## 开发工作流程

当我要求"创建新组件"或"添加新功能"时：

### 1️⃣ 需求理解（30 秒思考）

- 如果需求明确：直接进入第 2 步
- 如果需求模糊：向我提出 2-3 个关键问题：
  - 组件的用途和功能？
  - 需要哪些 props 和状态？
  - 是否需要与后端 API 交互？

### 2️⃣ 技术设计（快速规划）

- 确定组件类型（UI 组件、页面组件、业务组件）
- 规划状态管理策略（本地状态 vs 全局状态）
- 确定需要的 API 接口和数据类型
- 考虑组件的可复用性和可访问性

### 3️⃣ 代码生成（按顺序、一次性完成）

按以下顺序生成完整代码：

1. **类型定义**（如需要）
2. **API 服务**（如需要）
3. **自定义 Hooks**（如需要）
4. **组件实现**
5. **样式和主题适配**

**重要：**

- ✅ 所有代码必须完整可运行
- ✅ 必须使用下文的模板代码和最佳实践
- ✅ 包含完整的 TypeScript 类型定义
- ✅ 遵循 Tailwind CSS 和 Shadcn UI 规范
- ❌ 禁止使用占位符（如 // ... rest of code）

### 4️⃣ 代码说明（简短）

- 生成代码后，用 1-2 句话说明核心逻辑
- 如果有特殊设计，简单解释原因

## 项目信息

- **技术栈**: React 19.x, TypeScript 5.x, Vite 7.x, Tailwind CSS 4.x
- **UI 库**: Shadcn UI + Radix UI
- **状态管理**: Zustand + TanStack Query
- **路由**: React Router 7.x
- **表单**: React Hook Form + Zod
- **主题**: next-themes
- **包管理器**: pnpm
- **Node 版本**: >= 20.x
- **适用规模**: 中小型项目（5-20 个页面/功能模块）

## 项目结构

```
src/
├── components/           # 可复用组件
│   ├── ui/              # Shadcn UI 基础组件
│   ├── forms/           # 表单组件
│   └── layout/          # 布局组件
├── pages/               # 页面组件
├── hooks/               # 自定义 Hooks
├── stores/              # Zustand 状态管理
├── services/            # API 服务
├── types/               # TypeScript 类型定义
├── lib/                 # 工具函数和配置
├── contexts/            # React Context
└── assets/              # 静态资源
```

## 核心原则

1. **组件优先**：优先使用函数组件和 Hooks
2. **类型安全**：充分利用 TypeScript 的类型系统
3. **性能优化**：合理使用 memo、useMemo、useCallback
4. **可访问性**：遵循 WCAG 指南，使用语义化 HTML
5. **响应式设计**：移动端优先，渐进增强
6. **代码复用**：提取公共逻辑到自定义 Hooks
