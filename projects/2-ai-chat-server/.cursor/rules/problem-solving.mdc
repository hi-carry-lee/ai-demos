---
description: 问题解决策略和系统性分析方法
globs: ["**/*.ts"]
alwaysApply: true
---

# 问题解决策略

当遇到 Bug 或错误时：

## 第一次尝试

1. 阅读相关代码，理解上下文
2. 分析根本原因（而不是修改症状）
3. 提供修复方案

## 第二次尝试（如果第一次失败）

1. 重新审视问题（可能是理解偏差）
2. 检查是否遵循了技术规范
3. 提供修复方案 + 说明修改思路

## 第三次尝试（如果仍未解决）

启动系统性分析模式：

1. 列出 3 种可能的根本原因
2. 提供 3 种不同的解决方案
3. 说明每种方案的优缺点
4. 推荐最佳方案并说明理由

## 渐进式开发指导

### 新需求处理原则

#### 1️⃣ 优先扩展，而非重写

当我提出新需求时：

- 先判断：是扩展现有模块，还是创建新模块？
- 扩展现有模块的信号：
  - 功能属于同一个业务领域（如用户头像属于 user 模块）
  - 只是增加字段或方法
  - 不会让单个模块过于复杂（Service < 200 行）

示例：

```typescript
// ✅ 正确：扩展现有 Service
class UserService {
  // 现有方法
  async createUser() { ... }

  // 新增方法
  async updateAvatar(userId: string, avatarUrl: string) { ... }
}

// ❌ 错误：创建新模块
class AvatarService { ... } // 没必要
```

#### 2️⃣ 修改现有代码时的沟通

当需要修改现有代码时，必须：

1. **先说明影响范围**

   ```
   这个修改会影响：
   - user.service.ts 的 createUser 方法
   - user.schema.ts 需要新增验证规则
   - 不影响其他模块
   ```

2. **提供向后兼容方案（如果可能）**

   ```typescript
   // ✅ 向后兼容：新增可选参数
   async createUser(data: CreateUserInput, options?: { sendEmail?: boolean }) {
     // 默认行为不变
   }

   // ❌ 破坏性变更：修改必需参数
   async createUser(data: CreateUserInput, sendEmail: boolean) {
     // 会破坏现有调用
   }
   ```

3. **标注 API 变化**

   ```
   ⚠️ Breaking Changes:
   - POST /api/v1/users 现在需要 role 字段（必需）

   ✅ 非破坏性变化:
   - POST /api/v1/users 现在支持可选的 avatar 字段
   ```

#### 3️⃣ 何时创建新模块

只有满足以下条件时才创建新模块：

- 独立的业务领域（如 user 和 product 是独立的）
- 有自己的数据表
- 与现有模块耦合度低

**模块数量参考：**

- 5 个模块以下：非常健康
- 5-10 个模块：正常
- 10-15 个模块：考虑是否过度拆分
- 15 个以上：需要重新审视架构
