---
description: 模块开发标准流程和各层职责规范
globs:
  [
    "**/modules/**/*.ts",
    "**/*.schema.ts",
    "**/*.repository.ts",
    "**/*.service.ts",
    "**/*.controller.ts",
    "**/*.routes.ts",
  ]
alwaysApply: true
---

# 模块开发标准流程

## 开发顺序（中小型项目简化）

1. **schema.ts** - Zod 验证 + 类型定义（可合并在一个文件）
2. **repository.ts** - 数据库操作
3. **service.ts** - 业务逻辑
4. **controller.ts** - HTTP 处理
5. **routes.ts** - 路由定义

## 各层职责和规范

### 1. Schema + Types (user.schema.ts)

合并 Zod schema 和类型定义（中小型项目推荐）

```typescript
import { z } from "zod";

// Zod 验证 schema
export const createUserSchema = z.object({
  body: z.object({
    email: z.string().email("Invalid email format"),
    password: z.string().min(8, "Password must be at least 8 characters"),
    name: z.string().min(2).max(50),
  }),
});

export const getUserSchema = z.object({
  params: z.object({
    id: z.string().uuid("Invalid user ID"),
  }),
});

// 从 Zod 推导类型
export type CreateUserInput = z.infer<typeof createUserSchema>["body"];
export type GetUserParams = z.infer<typeof getUserSchema>["params"];
```

### 2. Repository 层

- **结构**：Class + 单例模式
- **职责**：仅包含数据库操作
- **命名**：CRUD 方法使用 create, findById, update, delete, findMany
- **返回**：使用 select 排除敏感字段
- **不抛出业务错误**

### 3. Service 层

- **结构**：Class + 单例模式
- **职责**：业务逻辑和验证
- **调用**：Repository 层
- **错误**：抛出 AppError
- **事务**：复杂业务使用 prisma.$transaction

### 4. Controller 层

- **结构**：Class，方法为箭头函数
- **包裹**：使用 asyncHandler
- **响应**：使用 ApiResponse.success()
- **类型**：Request<Params, ResBody, ReqBody>
- **返回**：Promise<void>

### 5. Routes

- **RESTful 设计**
- **中间件顺序**：验证 → 认证 → 控制器
- **使用**：validate() 和 authenticate

#### validate()的用法，以及和 authenticate 的顺序

好处是就不用在 controller 中校验了；因为那样还需要手动处理校验失败的返回数据；

```typescript
router.patch(
  "/:id",
  authenticate, // 先验证登录，如果需要的话
  validate(updateUserSchema), // ← 再使用 updateUserSchema
  userController.updateUser
);
```

## 代码生成指令

当我要求"创建一个新模块"时，请按照以下顺序生成完整代码：

1. Prisma Schema（如需要新表）
2. Schema + Types（合并在一个文件）
3. Repository
4. Service
5. Controller
6. Routes

### 生成代码要求：

- ✅ 完整可运行，无占位符
- ✅ 使用上述所有模板（ApiResponse, AppError, asyncHandler 等）
- ✅ 包含 JSDoc 注释
- ✅ 完整的错误处理
- ✅ 类型安全
